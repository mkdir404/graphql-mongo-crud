import React, { useState }  from 'react';

const NewClient = () => {

    const [count, setCount] = useState(0);

  return (
    <h2 className="text-center">New Cliente</h2>
  );
}

export default NewClient;